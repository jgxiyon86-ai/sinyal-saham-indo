# Roadmap Cepat

1. Setup project Android Studio (Kotlin).
2. Implement login admin (token based).
3. Form input sinyal + validasi.
4. Tombol "Send WA Blast" per batch signal.
5. Sinkron setting blast dari backend (read-only di app).
6. Build APK release dan install ke device.
